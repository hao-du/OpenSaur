[features]
multi_agent = true

[agents]
# Allow your named team agents to run in parallel with a little headroom.
max_threads = 8
# Keep delegation to one child level to avoid recursive fan-out.
max_depth = 1
# Explicit default timeout for CSV-based fan-out jobs.
job_max_runtime_seconds = 1800
