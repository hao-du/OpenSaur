# Profile & Settings

**Goal**: Manage user identity info and customize application behavior.

## Requirements
- **Profile**: View/update current profile info; used for UI elements (side menu).
- **Settings**: Manage app-level settings; persisted as JSON config.

## Acceptance Criteria
- [ ] View own profile data.
- [ ] Update and persist application settings.
