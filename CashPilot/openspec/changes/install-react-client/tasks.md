## 1. Project Scaffolding

- [x] 1.1 Navigate to `src/OpenSaur.CashPilot.Web/client`
- [x] 1.2 Run `npx create-vite ./ --template react-ts`
- [x] 1.3 Verify that `package.json` and `vite.config.ts` are created.

## 2. Cleanup

- [x] 2.1 Remove any placeholder files that are not needed (e.g. `src/assets/react.svg` if requested, though here we'll keep the standard boilerplate).
- [x] 2.2 Verify that the project starts correctly (run `npm install` and `npm run dev` briefly).
